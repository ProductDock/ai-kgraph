# IDEs
.idea/
.vscode/
*.iml

# macOS
.DS_Store

# Scratch files written by the CI workflows into the runner workspace root
# (spec-from-intent.yml, plan-ready.yml). Anchored to the root so they don't
# also match nested files, e.g. src/lib/content/body.md.
/changed.txt
/todo.txt
/specs.txt
/issues.txt
/concerns.md
/pr-body.md
/body.md

# Local Claude Code state (settings.local.json is per-machine; skills are shared)
.claude/settings.local.json

# Next.js / Node
node_modules/
.next/
out/
next-env.d.ts
.env*.local
coverage/
.turbo/
*.tsbuildinfo

# Regenerated by `next dev`/`next build` (see node_modules/next/dist/server/lib/
# generate-agent-files.js). Not adopted: CLAUDE.md is this project's one convention
# file, and two drift.
/AGENTS.md

# rtk (local CLI proxy) test report cache
.vitest/
